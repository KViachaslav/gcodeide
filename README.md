

```
arc_to_lines(center, radius, start_angle, end_angle, num_segments)
remove_close_points(points, threshold=0.02)
calculate_boundary_coordinates(x1, y1, x2, y2, width)
distance(point1, point2)
find_closest_pointt(lines, target_point,nums)
# возвращает точку, индекс линии, старт (1) или конец(0), и растояние до нее\
find_closest_lines(lines, target_point,nums)
rotate_points(points, center, angle_degrees)
rotating(tags,centerx,centery,angle)
midpoint_between_two_points(pointA, pointB)
```
